% my $message = stash 'message' or die 'No message sent';
<article>
% 	my ($user) = @{$message->authors};
	<p><a href="/profile/<%=$user->user_name // $user->uuid%>"><%= $user->user_name // $user->uuid %></a> wrote: <%= $message->text %></p>
</article>
